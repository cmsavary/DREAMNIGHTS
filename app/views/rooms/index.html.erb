<ul>
  <% @rooms.each do |room| %>
    <li><%= room.name %></li>
    <li><%= room.price_per_night %></li>
    <li><%= room.capacity %></li>
    <li><%= room.photo %></li>
    <li><%= link_to "Find a Dream Room", room_url(room), class: "btn btn-primary" %></li>
  <% end %>
</ul>
